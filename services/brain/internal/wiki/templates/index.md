# 页面索引

本页是项目所有页面的索引，按 frontmatter `type` 分组列出。新建页面后在此补一行，让项目结构一眼可查——页面类型集合见 [[页面规范]]。

## 条目格式

每页一行，slug 用 `[[反链]]`，后接一句话说明：

```
- [[slug]] — 一句话说明这页是什么
```

## 按类型分组

<!-- 新页面按 type 归入对应小节；没有该类型的页面就保持空缺。-->

（暂无页面——建好第一批页面后按类型回填本节。）
