---
name: update-of-target
description: A v1 skill that update_of-style proposals can target — exists so PS3.1 diff tests have a known prior version.
version: 1.0.0
---

This is the v1 prompt body. A subsequent `skill.propose(update_of=...)`
PR will rewrite this to v2; the diff view in the approval card surfaces
the body delta line-by-line.

Argument: `$ARGS`.
