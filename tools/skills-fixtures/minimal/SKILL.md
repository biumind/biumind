---
name: minimal
description: Absolute floor — frontmatter has only the two required fields.
---

Reply with the single word "OK". This skill exists so loader tests have a
known-good baseline that exercises the parser without any optional fields.
